# Source Inventory — Provenance and Irrelevant Files

This document records:

1. **What was copied into `quite_change/`** and which source file each came from.
2. **What is in the source project tree but is NOT used** by this final HTML system — i.e. what can be safely deleted from the surrounding project when you're ready to clean up.

Throughout this document, **source-root** means: `c:\Users\Sahal Saeed\Documents\tempest_ai\quick\New_quick\new_quick\` (the project root). All paths are relative to source-root.

---

## Part 1 — What was copied into quite_change/

The whole HTML system depends on **108 files** from one source directory.

### From `outputs/analyst_patterns/v0/` → `quite_change/`

| Source path | Destination | Purpose |
|---|---|---|
| `outputs/analyst_patterns/v0/_build_2025_reason_view.py` | `quite_change/build/build_view.py` | The whole engine (renamed). File-paths were updated to use the new subdirectory layout — the only edits during the copy. |
| `outputs/analyst_patterns/v0/company_research_2025.json` | `quite_change/data/company_research_2025.json` | Per-company JP+EN narratives + Round 2 addenda. |
| `outputs/analyst_patterns/v0/it_census_universe.csv` | `quite_change/data/it_census_universe.csv` | Ticker → name / sub_type / size_band registry. |
| `outputs/analyst_patterns/v0/parser_output_*.json` (100 files) | `quite_change/data/parser_outputs/parser_output_*.json` | Per-company fixture data (year-by-year structured records + raw prose). |
| `outputs/analyst_patterns/v0/verification_findings_round2.md` | `quite_change/audit/verification_findings_round2.md` | R+×S- Round 2 audit trail. |
| `outputs/analyst_patterns/v0/verification_findings_round2_RplusSplus.md` | `quite_change/audit/verification_findings_round2_RplusSplus.md` | R+×S+ Round 2 audit trail. |
| `outputs/analyst_patterns/v0/OPENING_REMARKS_2026-06-04.md` | `quite_change/deliverables/OPENING_REMARKS_2026-06-04.md` | Meeting talking points. |

Files NOT copied that are regenerated by the build script:

- `R_PLUS_2025_view.html` — written to `quite_change/deliverables/R_PLUS_2025_view.html` when you run the build.
- `R_PLUS_2025_view.csv` — written to `quite_change/deliverables/R_PLUS_2025_view.csv` when you run the build.

**No files outside `outputs/analyst_patterns/v0/` were used.** None of the root-level scripts, none of the other subdirectories (`app/`, `config/`, `data/`, `notebooks/`, `scripts/`, `tests/`, `venv/`, `.claude/`, etc.) are touched.

---

## Part 2 — What is in the source tree but NOT used by this system

### Source root — none of these files are used

These ~60 files live in the project root. None are read by `build_view.py`.

| Category | Files | Why not used |
|---|---|---|
| Configuration / dotfiles | `.env`, `.env.example`, `.gitignore` | Environment / git config — not relevant to this Python build script. |
| Logs | `benchmark*.log`, `edinet_*.log`, `fetch_*.log`, `histcheck.log`, `shorthist.log`, `universe_v*.log` | Output logs from earlier data-fetching runs. Historical only. |
| One-off verification scripts | `verify_3co.py`, `verify_4755.py`, `verify_6758_fresh.py`, `verify_9432.py`, `verify_9432_full.py`, `verify_9984.py`, `verify_9984_full.py`, `verify_foreign_tangent.py`, `verify_ntt_only.py`, `verify_quiet_change.py`, `verify_three_companies.py` | Ad-hoc diagnostics for specific tickers from earlier iterations. Not part of the HTML build path. |
| One-off output JSONs | `verify_3co_results.json`, `verify_ntt_only_output.json`, `verify_quiet_change_output.json` | Outputs from the verify scripts above. Historical. |
| Data-fetching scripts | `fetch_finance_peers.py`, `fetch_it_peers.py`, `fetch_logistics_peers.py`, `fetch_realestate_peers.py`, `fetch_tempest.py`, `peek_9984_divestiture.py` | Used to fetch the original financial data into the parser_output files. Those parser files are now snapshotted into `quite_change/data/parser_outputs/`, so the fetchers are no longer needed for the build. |
| Documents | `document1.md`, `document2.md`, `IT_Companies_JPX5250.docx`, `PHASE_ABC_SUMMARY.md`, `README.md` | Project-level docs — superseded by `quite_change/README.md` for this system. |
| Project setup | `conftest.py`, `requirements.txt` | pytest config + Python dependency list. Not needed — `build_view.py` only uses the standard library. |

### Subdirectories — none are used

| Directory | What it is | Used by this system? |
|---|---|---|
| `.claude/` | Claude Code configuration | No. |
| `.pytest_cache/` | Pytest cache | No. |
| `__pycache__/` | Python bytecode cache | No. |
| `app/` | Application code (parser, fetchers, etc.) — produces the parser_output files originally | No — outputs already snapshotted into `quite_change/data/parser_outputs/`. |
| `config/` | App config | No. |
| `data/` | Project-root data dir (yfinance cache, results, etc.) | No. |
| `notebooks/` | Jupyter notebooks | No. |
| `outputs/` | Generated outputs (across many analysis stages, including `analyst_patterns/v0/` from which we pulled) | Only `outputs/analyst_patterns/v0/` source files were used — and they've all been copied into `quite_change/`. The rest of `outputs/` is unused. |
| `scripts/` | Project scripts | No. |
| `tests/` | Project tests | No. |
| `venv/` | Python virtualenv | No — only the standard library is used. |

### Inside `outputs/analyst_patterns/v0/` itself — the 445 files there, vs. the 107 we used

The source folder `outputs/analyst_patterns/v0/` contains 445 files. We used 107 of them. The remaining **338 files** are leftover artifacts from earlier iterations and are NOT needed for the final HTML system. These fall into clearly-labelled categories:

| Pattern | Approx. count | What they are |
|---|---|---|
| `_add_v*.py`, `_append_batch*.py`, `_apply_round2_*.py`, `_backfill_*.py`, `_build_*.py` (other than the one we copied), `_debug_build.py`, `_compute_*.py`, `_outlier_*.py`, `_patch_*.py`, `_rederive_*.py`, `_refresh_*.py`, `_resynthesize_*.py`, `_sector*_stratify.py`, `_strip_alpha_*.py`, `_survey_library.py`, `_test_*.py`, `_year_aware_audit.py`, `_show_12m.py`, `_diagnostic_events.py`, `_itemize_events.py` | ~80 Python helpers | One-off scripts from earlier iteration cycles — already applied their changes into `company_research_2025.json` or other artifacts. Historical only. |
| `_v*_raw.csv`, `_batch*_selection.json`, `_v13_*.csv`, `_v16_*.csv`, ... `_v45_*.csv`, `_report_data*.json`, `_tickers_for_33sector.txt` | ~50 raw working files | Intermediate raw CSV / JSON pulls from earlier batches. The final cleaned data lives in `parser_output_*.json` and `company_research_2025.json`. |
| `backtest_events_expanded_v3.csv.bak_pre_v*` (many .bak files) + `backtest_events_expanded_v*.csv` | ~32 backup CSVs | Auto-saved backups of the rolling backtest events file. Not used by the build. |
| `PROJECT_STATUS_REPORT.html.bak_pre_v*` + `PROJECT_STATUS_REPORT_JP.html.bak_pre_v*` + `PROJECT_STATUS_REPORT.html.bak_v*` | ~50 HTML backups | Versioned backups of a different report (PROJECT_STATUS_REPORT, not R_PLUS_2025_view). The current `PROJECT_STATUS_REPORT.html` and `PROJECT_STATUS_REPORT_JP.html` are separate deliverables not used by this view. |
| `quiet_change_report.html.bak_pre_*` + `quiet_change_grid.html` | ~10 quiet-change report variants | Earlier quiet-change reports — different deliverable, not the R+ view. |
| Earlier-stage Markdown reports: `ACQUISITION_PLAYBOOK.md`, `BASE_RATE_2022_2024.md`, `BUILD_STATUS.md`, `CONVERGENCE_TEST_RESULTS.md`, `EMERGING_GAPS.md`, `EXEC_SUMMARY_2026-06-03.md`, `INTER_RATER_AGREEMENT.md`, `LIBRARY_SURVEY.md`, `NAKAMACHI_PITCH.md`, `OVERNIGHT_SUMMARY.md`, `PATTERN_QUALITY_ASSESSMENT.md`, `PATTERN_VALIDATION_FULL.md`, `PHASE2_*.md` (6 files), `PRINCIPLES.md`, `REDERIVE_REPORT_*.md` (2), `SCHEMA_LIGHT_FIX.md`, `SHIFT_REPORT_*.md` (2), `SLIDE_BULLETS_2026-06-03.md`, `SMALL_CAP_VALIDATION*.md` (2), `STEP1_DIRECTIVE.md`, `SURVEY_AT_CLOSE_BATCH*.md` (2), `TEST_RESULTS.md`, `TIME_PERIOD_*.md` (2), `TRAIN_VS_TEST_RESULTS.md`, `WEAK_PATTERNS_REFINEMENT.md`, `schema_v1.md`, `cap_band_stratification.md`, `company_history.md`, `sector_stratification.md`, `year_stratification.md` | ~45 working Markdown docs | Status / methodology / analysis docs from earlier project phases. Not consumed by the build. |
| Working JSONs / YAMLs / CSVs from earlier stages: `cap_band_lookup.py`, `cap_band_stratification.json`, `cap_band_stratify.py`, `cap_bands.json`, `canonical_block.py`, `company_history.json`, `company_history.py`, `company_year_classifications.json`, `compare_train_test.py`, `compound_reliable.json`, `convergence_meter.py`, `convergence_series.csv`, `events_p*.csv` (3), `ground_truth.json`, `hierarchy_v1.yaml`, `hybrid_runner.py`, `inject_hit_rates.py`, `it_census_universe.json` (the JSON sibling of the CSV), `needs_authenticated_source.json`, `outlier_audit.json`, `pattern_capband_year.json` / `.py`, `pilot_sample.json`, `prompts.py`, `quadrant_deriver.py`, `QUADRANT_VIEW_2026-06-03.csv` / `.html` / `.md`, `qualitative_patterns.yaml`, `quantitative_patterns_extended.yaml`, `ranker.py`, `run_test.py`, `sector_stratification.json`, `sector_stratify.py`, `sector_tuned_reliable.json`, `sector33_tuned_reliable.json`, `sector33_yearly_reliable.json`, `test_events.csv`, `ticker_sector33.csv`, `time_period_split.py`, `train_events.csv`, `train_test_split.py`, `unified_classifier.py`, `validate_agreement.py`, `year_aware_audit.json`, `year_stratification.json`, `year_stratify.py`, `broker_tape_parser.py`, `parser.py`, `acquisition_helpers.py`, `build_v3_csv.py`, `_backfill_canonical.py`, `_compound_patterns.py`, `_build_pilot_sample.py`, `_build_playbook.py`, `_build_horizon_section.py`, `_build_capband_toggle.py`, `_build_4quadrant_year.py`, `_build_batch2_fixtures.py`, `_build_batch3_fixtures.py`, `_build_it_census.py`, `_build_new_patterns_section.py`, `_build_quadrant_html.py`, `_build_quadrant_view.py`, `_build_sector_tuned_section.py`, `_build_sector33_section.py`, `_build_sector33_yearly_section.py`, `_build_validation.py`, `_build_year_aware_section.py` | ~70 working code/data files | Used during earlier project phases (pattern discovery, validation, sector stratification, etc.). The build script for the R+ view does NOT import or read any of these. |
| Fresh/temp parser outputs that ARE used: `parser_output_moneyforward_fresh.json`, `parser_output_squareenix_fresh.json`, `parser_output_sansan_fresh.json` | 3 files | These ARE referenced by the build script via the `preferred_map` override (lines 74-78 in build_view.py). Confirmed copied to `quite_change/data/parser_outputs/`. |
| Other 100 parser_output_*.json files | 100 files | Snapshotted into `quite_change/data/parser_outputs/`. |
| Other tracked files we copied: `company_research_2025.json`, `it_census_universe.csv`, `verification_findings_round2*.md`, `OPENING_REMARKS_2026-06-04.md`, `_build_2025_reason_view.py` | 7 files | All copied as documented in Part 1. |

### Other sibling files in source-root we don't need

Files like `INTER_RATER_AGREEMENT.md`, `IT_Companies_JPX5250.docx`, `requirements.txt`, etc. live in the project root and are not needed by the HTML build. Already covered in the "Source root" table above.

---

## Part 3 — When you're ready to clean up the surrounding project

Per your plan, the surrounding project tree will be cleaned up later. When that happens:

1. The contents of `quite_change/` (this folder) is the keep-set. Everything inside this directory is needed for the final HTML system.
2. Everything outside `quite_change/` can be reviewed for deletion. Specifically:
   - All ~338 unused files in `outputs/analyst_patterns/v0/` are safe to delete from a build-dependency perspective. Some may have sentimental / archival value — your call.
   - All files in source-root (the 60-or-so listed above) are unused by this system.
   - All subdirectories (`app/`, `config/`, `data/`, `notebooks/`, `scripts/`, `tests/`, `venv/`, etc.) are unused by this system.
3. When you manually move `quite_change/` contents out, just move the four subdirectories (`build/`, `data/`, `audit/`, `deliverables/`) plus `README.md` and `NOTES_source_inventory.md`. The rebuild command stays `python build/build_view.py` from wherever you place them.

---

## Verification

The standalone build was run from `quite_change/` on 2026-06-04. Result:

```
Wrote deliverables/R_PLUS_2025_view.html (1,271,683 bytes)
Total R+: 82 (41 S- + 41 S+)
Dropped R-: 14
Unclassified (dropped): 2
```

Identical to the build run from the original `outputs/analyst_patterns/v0/` directory. The split is byte-for-byte equivalent (same companies, same buckets, same counts, same content).
